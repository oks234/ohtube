extends ../base

block content
  if errorMessage
    small=errorMessage
  form(method="POST")
    input(type="password" name="oldPassword" placeholder="Old Password" required)
    input(type="password" name="newPassword" placeholder="New Password" required)
    input(type="password" name="newPasswordConfirmation" placeholder="New Password Confirmation" required)
    button(type="submit") Change Passsword